import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-doc-notifications',
  imports: [RouterLink],
  templateUrl: './notifications.html',
})
export class DocNotifications {}

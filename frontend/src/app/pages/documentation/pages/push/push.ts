import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-doc-push',
  imports: [RouterLink],
  templateUrl: './push.html',
})
export class DocPush {}

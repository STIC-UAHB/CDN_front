import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-doc-websocket',
  imports: [RouterLink],
  templateUrl: './websocket.html',
})
export class DocWebsocket {}

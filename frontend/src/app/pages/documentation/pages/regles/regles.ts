import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-doc-regles',
  imports: [RouterLink],
  templateUrl: './regles.html',
})
export class DocRegles {}
